import { CommandsPage } from "@/containers/commands/CommandsPage";

export const metadata = {
    title: "Commandes",
    description: "Page de gestion des commandes",
}

export default function Commands() {

    return (
        <CommandsPage />
    )
}